#!/usr/bin/env pwsh
# PowerShell script for new_plugin_with_types template

param(
    [Parameter(Mandatory)]
    [string] $crate_name,
    [Parameter(Mandatory)]
    [string] $crate_name_pascal,
)

# Your script logic here, using variables like $crate_name
